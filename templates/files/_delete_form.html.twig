<form method="post" action="{{ path('delete_file', {'id': file.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce fichier ? (l action est irreversible)');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ file.id) }}">
    <button class="btn">Delete</button>
</form>
